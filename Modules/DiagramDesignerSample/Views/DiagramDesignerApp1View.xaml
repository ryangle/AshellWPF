<UserControl
    x:Class="DiagramDesignerSample.Views.DiagramDesignerApp1View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="clr-namespace:DiagramDesigner;assembly=DiagramDesigner"
    xmlns:local="clr-namespace:DiagramDesignerSample.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="White"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/DiagramDesignerSample;component/Resources/Shared.xaml" />
                <ResourceDictionary Source="/DiagramDesignerSample;component/Resources/ApplicationToolbar.xaml" />
                <ResourceDictionary Source="/DiagramDesignerSample;component/Resources/Stencils/FlowChartStencils.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <ContextMenu x:Key="DesignerCanvasContextMenu">
                <MenuItem Command="{x:Static ApplicationCommands.Paste}" Header="Paste">
                    <MenuItem.Icon>
                        <Image Width="16" Source="/Resources/Images/Paste.png" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Command="{x:Static dd:DesignerCanvas.SelectAll}" Header="Select All" />
            </ContextMenu>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ContentControl Content="{StaticResource MyToolbar}" />

        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="265" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <!--  Toolbox  -->
            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                <Expander
                    Content="{StaticResource FlowChartStencils}"
                    Header="Flow Chart"
                    IsExpanded="True" />
            </StackPanel>
            <!--  GridSplitter  -->
            <GridSplitter
                Width="2"
                HorizontalAlignment="Right"
                VerticalAlignment="Stretch"
                Background="LightGray"
                Focusable="False" />
            <!--  Designer  -->
            <GroupBox
                Grid.Column="1"
                Margin="3,0,0,0"
                Header="Diagram">
                <TabControl>
                    <TabItem Header="Designer">
                        <Grid>
                            <ScrollViewer
                                Name="DesignerScrollViewer"
                                Background="Transparent"
                                HorizontalScrollBarVisibility="Auto"
                                RequestBringIntoView="DesignerScrollViewer_RequestBringIntoView"
                                VerticalScrollBarVisibility="Auto">
                                <dd:DesignerCanvas
                                    x:Name="MyDesigner"
                                    Margin="10"
                                    Background="{StaticResource WindowBackgroundBrush}"
                                    ContextMenu="{StaticResource DesignerCanvasContextMenu}"
                                    FocusVisualStyle="{x:Null}"
                                    Focusable="true"
                                    SelectionNeedsCtrl="True" />
                            </ScrollViewer>
                            <dd:ZoomBox
                                Width="180"
                                Margin="0,5,25,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                ScrollViewer="{Binding ElementName=DesignerScrollViewer}" />
                        </Grid>
                    </TabItem>
                    <TabItem Header="Test">aaa</TabItem>
                </TabControl>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
