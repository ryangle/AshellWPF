<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:DiagramDesigner.Controls;assembly=DiagramDesigner"
    xmlns:controls="clr-namespace:DiagramDesignerSample.Controls"
    xmlns:d="clr-namespace:DiagramDesigner;assembly=DiagramDesigner"
    xmlns:s="clr-namespace:DiagramDesigner.Behaviors;assembly=DiagramDesigner">

    <Brush x:Key="ItemStroke">#FFD69436</Brush>
    <LinearGradientBrush x:Key="ItemBrush" StartPoint="0,0" EndPoint="0,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Offset="0" Color="#FAFBE9" />
            <GradientStop Offset="1" Color="Orange" />
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <Brush x:Key="ItemStroke1">#ACADCD</Brush>
    <LinearGradientBrush x:Key="ItemBrush1" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Offset="0" Color="#FEFEFE" />
        <GradientStop Offset="1" Color="#BDBEDE" />
    </LinearGradientBrush>


    <Style x:Key="FlowChartItemStyle" TargetType="Path">
        <Setter Property="Fill" Value="{StaticResource ItemBrush}" />
        <Setter Property="Stroke" Value="{StaticResource ItemStroke}" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="StrokeLineJoin" Value="Round" />
        <Setter Property="Stretch" Value="Fill" />
        <Setter Property="IsHitTestVisible" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <!--<Setter Property="BitmapEffect">
      <Setter.Value>
        <DropShadowBitmapEffect Color="#AAA" Direction="315" ShadowDepth="10"
                                Softness="0.5" Opacity="0.6"/>
      </Setter.Value>
    </Setter>-->
    </Style>

    <!--  Process  -->
    <Style
        x:Key="Process"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0,0 H 60 V40 H 0 Z" />
    </Style>

    <Style
        x:Key="Process_DragThumb"
        BasedOn="{StaticResource Process}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  Decision  -->
    <Style
        x:Key="Decision"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0,20 L 30 0 L 60,20 L 30,40 Z" />
    </Style>

    <Style
        x:Key="Decision_DragThumb"
        BasedOn="{StaticResource Decision}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  Document  -->
    <Style
        x:Key="Document"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0,0 H 60 V 40 C 30,30 30,50 0,40 Z" />
    </Style>

    <Style
        x:Key="Document_DragThumb"
        BasedOn="{StaticResource Document}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  Data  -->
    <Style
        x:Key="Data"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 10,0 L 60 0 L 50,40 L 0,40 Z" />
    </Style>

    <Style
        x:Key="Data_DragThumb"
        BasedOn="{StaticResource Data}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  Start  -->
    <Style
        x:Key="Start"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 10,20 A 20,20 0 1 1 50,20 A 20,20 0 1 1 10,20" />
    </Style>

    <Style
        x:Key="Start_DragThumb"
        BasedOn="{StaticResource Start}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>


    <!--  Predefined  -->
    <Style
        x:Key="Predefined"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 50,0 V 40 M 10,0 V 40 M 0 0 H 60 V 40 H 0 Z" />
    </Style>

    <Style
        x:Key="Predefined_DragThumb"
        BasedOn="{StaticResource Predefined}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>


    <!--  StoredData  -->
    <Style
        x:Key="StoredData"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 5,0 H 60 A 40,40 0 0 0 60,40 H 5 A 40,40 0 0 1 5,0 Z" />
    </Style>

    <Style
        x:Key="StoredData_DragThumb"
        BasedOn="{StaticResource StoredData}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>


    <!--  InternalStorage  -->
    <Style
        x:Key="InternalStorage"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0,10 H 60 M 10,0 V 40 M 0,0 H 60 V 40 H 0 Z" />
    </Style>

    <Style
        x:Key="InternalStorage_DragThumb"
        BasedOn="{StaticResource InternalStorage}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  SequentialData  -->
    <Style
        x:Key="SequentialData"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 30,40 A 20,20 0 1 1 30,0 A 20,20 0 0 1 43,35 H 50 L 50,40 Z" />
    </Style>

    <Style
        x:Key="SequentialData_DragThumb"
        BasedOn="{StaticResource SequentialData}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  DirectData  -->
    <Style
        x:Key="DirectData"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="F 1 M 57,40 H 3 A 4,20 0 1 1 3,0 H 57 A 4,20.1 0 1 1 56,0" />
    </Style>

    <Style
        x:Key="DirectData_DragThumb"
        BasedOn="{StaticResource DirectData}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  ManualInput  -->
    <Style
        x:Key="ManualInput"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0 10 L 60,0 V 40 H 0 Z" />
    </Style>

    <Style
        x:Key="ManualInput_DragThumb"
        BasedOn="{StaticResource ManualInput}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  Card  -->
    <Style
        x:Key="Card"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0 10 L 10,0 H 60 V 40 H 0 Z" />
    </Style>

    <Style
        x:Key="Card_DragThumb"
        BasedOn="{StaticResource Card}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  PaperTape  -->
    <Style
        x:Key="PaperTape"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0,3 C 30,-7 30,13 60,3 V 37 C 30,47 30,27 0,37 Z" />
    </Style>

    <Style
        x:Key="PaperTape_DragThumb"
        BasedOn="{StaticResource PaperTape}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>


    <!--  Delay  -->
    <Style
        x:Key="Delay"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0,0 H 40 A 20,20 0 0 1 40,40 H 0 Z" />
    </Style>

    <Style
        x:Key="Delay_DragThumb"
        BasedOn="{StaticResource Delay}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>


    <!--  Terminator  -->
    <Style
        x:Key="Terminator"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 20,40 A 20,20 0 0 1 20,0 H 40 A 20,20 0 0 1 40,40 Z" />
    </Style>

    <Style
        x:Key="Terminator_DragThumb"
        BasedOn="{StaticResource Terminator}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  Display  -->
    <Style
        x:Key="Display"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0,20 A 40,40 0 0 1 15,0 H 55 A 60,60 0 0 1 55,40 H 15 A 40,40, 0 0 1 0,20 Z" />
    </Style>

    <Style
        x:Key="Display_DragThumb"
        BasedOn="{StaticResource Display}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  LoopLimit  -->
    <Style
        x:Key="LoopLimit"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0 10 L 10,0 H 50 L 60,10 V 40 H 0 Z" />
    </Style>

    <Style
        x:Key="LoopLimit_DragThumb"
        BasedOn="{StaticResource LoopLimit}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  Preparation  -->
    <Style
        x:Key="Preparation"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0,20 L 10,0  H 50 L 60,20 L 50,40 H10 Z" />
    </Style>

    <Style
        x:Key="Preparation_DragThumb"
        BasedOn="{StaticResource Preparation}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  ManualOperation  -->
    <Style
        x:Key="ManualOperation"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0 0 H 60 L 50 40 H 10 Z" />
    </Style>

    <Style
        x:Key="ManualOperation_DragThumb"
        BasedOn="{StaticResource ManualOperation}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  OffPageReference  -->
    <Style
        x:Key="OffPageReference"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 0 0 H 60 V 20 L 30,40 L 0,20 Z" />
    </Style>

    <Style
        x:Key="OffPageReference_DragThumb"
        BasedOn="{StaticResource OffPageReference}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <!--  Star  -->
    <Style
        x:Key="Star"
        BasedOn="{StaticResource FlowChartItemStyle}"
        TargetType="Path">
        <Setter Property="Data" Value="M 9,2 11,7 17,7 12,10 14,15 9,12 4,15 6,10 1,7 7,7 Z" />
    </Style>

    <Style
        x:Key="Star_DragThumb"
        BasedOn="{StaticResource Star}"
        TargetType="Path">
        <Setter Property="IsHitTestVisible" Value="true" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke" Value="Transparent" />
    </Style>

    <d:Toolbox
        x:Key="FlowChartStencils"
        ItemSize="60,50"
        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
        SnapsToDevicePixels="True">
        <ItemsControl.Items>

            <controls:Test1 d:ToolboxItem.DesiredSize="88,130" d:ToolboxItem.InsertInBackground="True" />

            <controls:Test1 d:ToolboxItem.DesiredSize="200,130" d:ToolboxItem.InsertInBackground="True" />

            <Path Style="{StaticResource Process}" ToolTip="Process">
                <!--<s:DesignerItem.DragThumbTemplate>
          <ControlTemplate>
            <Path Style="{StaticResource Process_DragThumb}"/>
          </ControlTemplate>
        </s:DesignerItem.DragThumbTemplate>-->
            </Path>

            <Path Style="{StaticResource Decision}" ToolTip="Decision">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource Decision_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource Document}" ToolTip="Document">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource Document_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
                <d:DesignerItem.ConnectorDecoratorTemplate>
                    <ControlTemplate>
                        <c:RelativePositionPanel Margin="-4">
                            <d:Connector
                                x:Name="Top"
                                c:RelativePositionPanel.RelativePosition="0.5,0"
                                Orientation="Top" />
                            <d:Connector
                                x:Name="Left"
                                c:RelativePositionPanel.RelativePosition="0,0.5"
                                Orientation="Left" />
                            <d:Connector
                                x:Name="Right"
                                c:RelativePositionPanel.RelativePosition="1,0.5"
                                Orientation="Right" />
                            <d:Connector
                                x:Name="Bottom"
                                c:RelativePositionPanel.RelativePosition="0.5,0.93"
                                Orientation="Bottom" />
                        </c:RelativePositionPanel>
                    </ControlTemplate>
                </d:DesignerItem.ConnectorDecoratorTemplate>
            </Path>

            <Path Style="{StaticResource Data}" ToolTip="Data">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource Data_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
                <d:DesignerItem.ConnectorDecoratorTemplate>
                    <ControlTemplate>
                        <c:RelativePositionPanel Margin="-4">
                            <d:Connector
                                x:Name="Top"
                                c:RelativePositionPanel.RelativePosition="0.5,0"
                                Orientation="Top" />
                            <d:Connector
                                x:Name="Left"
                                c:RelativePositionPanel.RelativePosition="0.09,0.5"
                                Orientation="Left" />
                            <d:Connector
                                x:Name="Right"
                                c:RelativePositionPanel.RelativePosition="0.91,0.5"
                                Orientation="Right" />
                            <d:Connector
                                x:Name="Bottom"
                                c:RelativePositionPanel.RelativePosition="0.5,1"
                                Orientation="Bottom" />
                        </c:RelativePositionPanel>
                    </ControlTemplate>
                </d:DesignerItem.ConnectorDecoratorTemplate>
            </Path>

            <Path Style="{StaticResource Start}" ToolTip="Start">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource Start_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource PaperTape}" ToolTip="Paper Tape">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource PaperTape_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
                <d:DesignerItem.ConnectorDecoratorTemplate>
                    <ControlTemplate>
                        <c:RelativePositionPanel Margin="-4">
                            <d:Connector
                                x:Name="Top"
                                c:RelativePositionPanel.RelativePosition="0.5,0.07"
                                Orientation="Top" />
                            <d:Connector
                                x:Name="Left"
                                c:RelativePositionPanel.RelativePosition="0,0.5"
                                Orientation="Left" />
                            <d:Connector
                                x:Name="Right"
                                c:RelativePositionPanel.RelativePosition="1,0.5"
                                Orientation="Right" />
                            <d:Connector
                                x:Name="Bottom"
                                c:RelativePositionPanel.RelativePosition="0.5,0.93"
                                Orientation="Bottom" />
                        </c:RelativePositionPanel>
                    </ControlTemplate>
                </d:DesignerItem.ConnectorDecoratorTemplate>
            </Path>

            <Path Style="{StaticResource Predefined}" ToolTip="Predefined">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource Predefined_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource StoredData}" ToolTip="Stored Data">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource StoredData_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
                <d:DesignerItem.ConnectorDecoratorTemplate>
                    <ControlTemplate>
                        <c:RelativePositionPanel Margin="-4">
                            <d:Connector
                                x:Name="Top"
                                c:RelativePositionPanel.RelativePosition="0.5,0"
                                Orientation="Top" />
                            <d:Connector
                                x:Name="Left"
                                c:RelativePositionPanel.RelativePosition="0,0.5"
                                Orientation="Left" />
                            <d:Connector
                                x:Name="Right"
                                c:RelativePositionPanel.RelativePosition="0.9,0.5"
                                Orientation="Right" />
                            <d:Connector
                                x:Name="Bottom"
                                c:RelativePositionPanel.RelativePosition="0.5,1"
                                Orientation="Bottom" />
                        </c:RelativePositionPanel>
                    </ControlTemplate>
                </d:DesignerItem.ConnectorDecoratorTemplate>
            </Path>

            <Path Style="{StaticResource InternalStorage}" ToolTip="Internal Storage">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource InternalStorage_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource SequentialData}" ToolTip="Sequential Data">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource SequentialData_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource DirectData}" ToolTip="Direct Data">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource DirectData_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource ManualInput}" ToolTip="Manual Input">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource ManualInput_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
                <d:DesignerItem.ConnectorDecoratorTemplate>
                    <ControlTemplate>
                        <c:RelativePositionPanel Margin="-4">
                            <d:Connector
                                x:Name="Top"
                                c:RelativePositionPanel.RelativePosition="0.5,0.12"
                                Orientation="Top" />
                            <d:Connector
                                x:Name="Left"
                                c:RelativePositionPanel.RelativePosition="0,0.5"
                                Orientation="Left" />
                            <d:Connector
                                x:Name="Right"
                                c:RelativePositionPanel.RelativePosition="1,0.5"
                                Orientation="Right" />
                            <d:Connector
                                x:Name="Bottom"
                                c:RelativePositionPanel.RelativePosition="0.5,1"
                                Orientation="Bottom" />
                        </c:RelativePositionPanel>
                    </ControlTemplate>
                </d:DesignerItem.ConnectorDecoratorTemplate>
            </Path>

            <Path Style="{StaticResource Card}" ToolTip="Card">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource Card_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource Delay}" ToolTip="Delay">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource Delay_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource Terminator}" ToolTip="Terminator">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource Terminator_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource Display}" ToolTip="Display">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource Display_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource LoopLimit}" ToolTip="Loop Limit">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource LoopLimit_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource Preparation}" ToolTip="Preparation">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource Preparation_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

            <Path Style="{StaticResource ManualOperation}" ToolTip="Manual Operation">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource ManualOperation_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
                <d:DesignerItem.ConnectorDecoratorTemplate>
                    <ControlTemplate>
                        <c:RelativePositionPanel Margin="-4">
                            <d:Connector
                                x:Name="Top"
                                c:RelativePositionPanel.RelativePosition="0.5,0"
                                Orientation="Top" />
                            <d:Connector
                                x:Name="Left"
                                c:RelativePositionPanel.RelativePosition="0.1,0.5"
                                Orientation="Left" />
                            <d:Connector
                                x:Name="Right"
                                c:RelativePositionPanel.RelativePosition="0.9,0.5"
                                Orientation="Right" />
                            <d:Connector
                                x:Name="Bottom"
                                c:RelativePositionPanel.RelativePosition="0.5,1"
                                Orientation="Bottom" />
                        </c:RelativePositionPanel>
                    </ControlTemplate>
                </d:DesignerItem.ConnectorDecoratorTemplate>
            </Path>

            <Path Style="{StaticResource OffPageReference}" ToolTip="Off Page Reference">
                <d:DesignerItem.DragThumbTemplate>
                    <ControlTemplate>
                        <Path Style="{StaticResource OffPageReference_DragThumb}" />
                    </ControlTemplate>
                </d:DesignerItem.DragThumbTemplate>
            </Path>

        </ItemsControl.Items>
    </d:Toolbox>
</ResourceDictionary>
